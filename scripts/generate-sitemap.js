/**
 * Next.js generates /sitemap.xml automatically at build/runtime via
 * app/sitemap.ts. This script is kept as a convenience placeholder
 * if you ever need to pre-generate a static sitemap for a non-Next.js
 * host. For normal Vercel deployments, no action is needed.
 */
console.log("Sitemap is generated automatically by app/sitemap.ts at /sitemap.xml");
